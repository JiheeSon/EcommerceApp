<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:padding="@dimen/_10dp"
        android:background="@drawable/background_gray_boundary"
        android:layout_marginBottom="@dimen/_10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/text_review_user_name"
            tools:text="John Parker"
            android:textSize="@dimen/_15sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <RatingBar
            android:id="@+id/rading_bar_review"
            android:isIndicator="true"
            android:numStars="5"
            android:stepSize="0.5"
            android:max="5"
            style="?android:attr/ratingBarStyleSmall"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/text_review_user_name"
            android:layout_width="wrap_content"
            android:layout_height="0dp"/>

        <TextView
            android:id="@+id/text_review_title"
            tools:text="Good quality phone"
            android:layout_marginTop="@dimen/_5dp"
            android:textColor="@color/light_orange"
            android:textSize="@dimen/_20sp"
            app:layout_constraintTop_toBottomOf="@id/text_review_user_name"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <TextView
            android:layout_marginTop="@dimen/_5dp"
            android:id="@+id/text_review_body"
            tools:text="One Plus always exports premium quality mobiles. Let there be no such features like other mobile but iPhone also doesn't export such features at high price."
            app:layout_constraintTop_toBottomOf="@id/text_review_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>